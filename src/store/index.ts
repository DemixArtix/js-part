import { createStore } from 'vuex'

import cart from './cart'
import list from './list'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    cart,
    list,
  }
})
